result
.direnv

# Terraform
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/terraform.tfvars
terraform/tfplan*
terraform/.terraform.lock.hcl

# Dependencies
node_modules/

# Build output is committed (nix sandbox can't fetch deps)
# Run `bun run build` before committing

# Secrets (plaintext) — encrypted yaml is committed
*.key
tunnel-env
